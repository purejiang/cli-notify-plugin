{
  "name": "cli-notify-plugin",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin for real-time session mirroring to Android via Cloud Relay",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
